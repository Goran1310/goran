
//What is their lexicographic order?
//Concatenate all the strings into a “Hello My Pretty World” sentence.

const a = 'Hello';
const b = 'My';
const c = 'Pretty';
const d = 'World';
console.log(a > b); //false
console.log(b > c); //true
console.log(a > c); //false
console.log(a < b); //true
console.log(b < c); //false
console.log(a < c); //true
const res = a + ' ' + b + ' ' + c + ' ' + d;


const Statement1 = weightComparison ? 'True — elephants weigh less than mice!?' : 'False — of course an elephant is heavier than a mouse!';
const Statement2 = heightComparison ? 'True — an ostrich is indeed taller than a duck!' : 'False — apparently a duck is taller than an ostrich!?';
const Statement3 = console.log(res);
if (a<b && b<c && c<d) {
    console.log(true);
} else {
    false
};

