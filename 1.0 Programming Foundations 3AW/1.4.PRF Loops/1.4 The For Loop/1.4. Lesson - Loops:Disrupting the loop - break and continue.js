
let array = Number.POSITIVE_INFINITY;
let userNumb = parseInt(prompt('User need to pass an even number.');

for ( let i = 0; i < array.length; i++) {
    if (let x = )
} 



// Create an infinite loop and ask the user (via prompt) to pass an even number. 
// If the number was even, end the loop and show that number in the alert. 
// Otherwise, show another prompt and ask for an even number again.

while (true) {
    let userNumb = prompt('User need to pass an even number.');
    if(userNumb % 2 === 0) {
        alert (userNumb);
        break;
    }
}
